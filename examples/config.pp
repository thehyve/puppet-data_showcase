# 
# An example of a manifest file which creates the datashowcase system user
# and generates configuration files for the data showcase application.
# Services are not installed.
#

include ::data_showcase
include ::data_showcase::config

